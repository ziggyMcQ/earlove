# ─── Spotify OAuth ────────────────────────────────────────────
# Create an app at https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# Must match the Redirect URI you set in the Spotify Dashboard.
# For local dev:   http://localhost:3000/api/auth/callback
# For production:  https://your-app.vercel.app/api/auth/callback
SPOTIFY_REDIRECT_URI=http://localhost:3000/api/auth/callback

# ─── NextAuth ─────────────────────────────────────────────────
# Generate with:  openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# ─── Optional ─────────────────────────────────────────────────
# Customize the app name shown in the UI (defaults to "earlove")
# NEXT_PUBLIC_APP_NAME=earlove
